name: Site tests

on:
  workflow_dispatch:
  push:
    branches: [preview]
  pull_request:
    branches: [preview]

permissions:
  contents: read
  actions: read
  issues: write

concurrency:
  group: service-menage-site-tests-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: false

env:
  NODE_VERSION: 24
  TEST_BASE_URL: http://127.0.0.1:4173

jobs:
  run-1-static-validation:
    name: Run 1 — Static site validation
    runs-on: ubuntu-latest
    timeout-minutes: 8
    steps:
      - name: Checkout preview
        uses: actions/checkout@v4
        with:
          ref: preview

      - name: Check JavaScript syntax
        run: node --check script.js

      - name: Check required files
        run: |
          set -e
          for file in index.html prestations.html apropos.html avis.html devis.html contact.html mentions-legales.html confidentialite.html styles.css script.js hero-bordeaux-menage.png portrait-menage-bordeaux.png package.json playwright.config.js tests/essential-site.spec.js; do
            if [ ! -f "$file" ]; then
              echo "Missing required file: $file"
              exit 1
            fi
          done

      - name: Check current branding and placeholders
        run: |
          set -e
          if grep -R -n "Laetitia\|Letícia\|Nykuto Services\|votre.email@gmail.com\|CONTACT_EMAIL" *.html script.js; then
            echo "Old branding or placeholder found."
            exit 1
          fi
          grep -q "Leticia" index.html

      - name: Check forms and WhatsApp wiring
        run: |
          set -e
          grep -q 'const WHATSAPP_NUMBER = "33768345608"' script.js
          grep -q 'window.open(url, "_blank", "noopener,noreferrer")' script.js
          grep -q 'zoneSelect?.addEventListener("change"' script.js
          grep -q 'serviceMap' script.js
          grep -q 'id="quote-form"' devis.html
          grep -q 'id="review-form"' avis.html
          grep -q 'https://wa.me/33768345608' contact.html
          for key in menage-regulier menage-ponctuel repassage grand-nettoyage; do
            grep -q "devis.html?service=$key" prestations.html
            grep -q "\"$key\"" script.js
          done

      - name: Check internal links and resources
        run: |
          node <<'NODE'
          const fs = require('fs');
          const pages = ['index.html','prestations.html','apropos.html','avis.html','devis.html','contact.html','mentions-legales.html','confidentialite.html'];
          let failed = false;
          for (const page of pages) {
            const html = fs.readFileSync(page, 'utf8');
            for (const match of html.matchAll(/(?:href|src)="([^"]+)"/g)) {
              const ref = match[1];
              if (/^(https?:|mailto:|tel:|#)/.test(ref)) continue;
              const target = ref.split('?')[0].split('#')[0];
              if (target && !fs.existsSync(target)) {
                console.error(`${page} links to missing resource: ${ref}`);
                failed = true;
              }
            }
          }
          if (failed) process.exit(1);
          console.log('All internal links and resources are valid.');
          NODE

  run-2-static-seo-content:
    name: Run 2 — Static SEO and content sanity
    needs: run-1-static-validation
    if: ${{ always() }}
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Checkout preview
        uses: actions/checkout@v4
        with:
          ref: preview

      - name: Check titles and meta descriptions
        run: |
          node <<'NODE'
          const fs = require('fs');
          const pages = ['index.html','prestations.html','apropos.html','avis.html','devis.html','contact.html','mentions-legales.html','confidentialite.html'];
          let failed = false;

          function decodeHtml(value) {
            return String(value || '')
              .replace(/&amp;/g, '&')
              .replace(/&quot;/g, '"')
              .replace(/&#39;/g, "'")
              .replace(/&nbsp;/g, ' ')
              .trim();
          }

          function getTitle(html) {
            return decodeHtml(html.match(/<title>([\s\S]*?)<\/title>/i)?.[1] || '');
          }

          function getDescription(html) {
            const nameFirst = html.match(/<meta\s+[^>]*name=["']description["'][^>]*content=["']([^"']+)["'][^>]*>/i)?.[1];
            const contentFirst = html.match(/<meta\s+[^>]*content=["']([^"']+)["'][^>]*name=["']description["'][^>]*>/i)?.[1];
            return decodeHtml(nameFirst || contentFirst || '');
          }

          for (const page of pages) {
            const html = fs.readFileSync(page, 'utf8');
            const title = getTitle(html);
            const description = getDescription(html);
            console.log(`${page}`);
            console.log(`  title: ${title}`);
            console.log(`  description: ${description}`);

            if (title.length < 8) {
              console.error(`SEO error in ${page}: missing or too short <title>.`);
              failed = true;
            }
            if (description.length < 20) {
              console.error(`SEO error in ${page}: missing or too short meta description.`);
              failed = true;
            }
            if (!/(Leticia|Bordeaux|ménage|menage)/i.test(`${title} ${description}`)) {
              console.error(`SEO error in ${page}: title/description should mention Leticia, Bordeaux, or ménage.`);
              failed = true;
            }
          }

          if (failed) process.exit(1);
          console.log('All static SEO checks passed.');
          NODE

  run-3-browser-smoke:
    name: Run 3 — Browser smoke tests
    needs: run-1-static-validation
    if: ${{ always() }}
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
        with:
          ref: preview
      - uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
      - run: npm install
      - name: Install Playwright Chromium with timeout
        run: timeout 300s npx playwright install --with-deps chromium
      - name: Start local static server
        run: |
          npx http-server . -p 4173 -s > server.log 2>&1 &
          for i in {1..30}; do
            if curl -fsS $TEST_BASE_URL/index.html > /dev/null; then
              echo "Static server is ready."
              exit 0
            fi
            sleep 1
          done
          echo "Static server did not start."
          cat server.log
          exit 1
      - name: Run smoke page-load tests
        run: |
          set +e
          npx playwright test tests/essential-site.spec.js --project=chromium --grep "\\[smoke\\]" --reporter=json > smoke.json
          code=$?
          node - smoke.json <<'NODE'
          const fs = require('fs');
          const file = process.argv[2];
          try {
            const report = JSON.parse(fs.readFileSync(file, 'utf8'));
            const stats = report.stats || {};
            console.log(`Playwright summary for ${file}: expected=${stats.expected ?? 0}, unexpected=${stats.unexpected ?? 0}, skipped=${stats.skipped ?? 0}, flaky=${stats.flaky ?? 0}`);
          } catch (error) {
            console.log(`Could not read ${file}: ${error.message}`);
          }
          NODE
          exit $code
      - name: Upload smoke evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: smoke-evidence
          if-no-files-found: ignore
          path: |
            smoke.json
            server.log
            test-results/**
            playwright-report/**

  run-4-browser-navigation:
    name: Run 4 — Browser navigation and service links
    needs: run-1-static-validation
    if: ${{ always() }}
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
        with:
          ref: preview
      - uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
      - run: npm install
      - name: Install Playwright Chromium with timeout
        run: timeout 300s npx playwright install --with-deps chromium
      - name: Start local static server
        run: |
          npx http-server . -p 4173 -s > server.log 2>&1 &
          for i in {1..30}; do
            if curl -fsS $TEST_BASE_URL/index.html > /dev/null; then
              echo "Static server is ready."
              exit 0
            fi
            sleep 1
          done
          echo "Static server did not start."
          cat server.log
          exit 1
      - name: Run navigation tests
        run: |
          set +e
          npx playwright test tests/essential-site.spec.js --project=chromium --grep "\\[navigation\\]" --reporter=json > navigation.json
          code=$?
          node - navigation.json <<'NODE'
          const fs = require('fs');
          const file = process.argv[2];
          try {
            const report = JSON.parse(fs.readFileSync(file, 'utf8'));
            const stats = report.stats || {};
            console.log(`Playwright summary for ${file}: expected=${stats.expected ?? 0}, unexpected=${stats.unexpected ?? 0}, skipped=${stats.skipped ?? 0}, flaky=${stats.flaky ?? 0}`);
          } catch (error) {
            console.log(`Could not read ${file}: ${error.message}`);
          }
          NODE
          exit $code
      - name: Upload navigation evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: navigation-evidence
          if-no-files-found: ignore
          path: |
            navigation.json
            server.log
            test-results/**
            playwright-report/**

  run-5-browser-mobile:
    name: Run 5 — Browser mobile menu tests
    needs: run-1-static-validation
    if: ${{ always() }}
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
        with:
          ref: preview
      - uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
      - run: npm install
      - name: Install Playwright Chromium with timeout
        run: timeout 300s npx playwright install --with-deps chromium
      - name: Start local static server
        run: |
          npx http-server . -p 4173 -s > server.log 2>&1 &
          for i in {1..30}; do
            if curl -fsS $TEST_BASE_URL/index.html > /dev/null; then
              echo "Static server is ready."
              exit 0
            fi
            sleep 1
          done
          echo "Static server did not start."
          cat server.log
          exit 1
      - name: Run mobile menu tests
        run: |
          set +e
          npx playwright test tests/essential-site.spec.js --project=chromium --grep "\\[mobile\\]" --reporter=json > mobile.json
          code=$?
          node - mobile.json <<'NODE'
          const fs = require('fs');
          const file = process.argv[2];
          try {
            const report = JSON.parse(fs.readFileSync(file, 'utf8'));
            const stats = report.stats || {};
            console.log(`Playwright summary for ${file}: expected=${stats.expected ?? 0}, unexpected=${stats.unexpected ?? 0}, skipped=${stats.skipped ?? 0}, flaky=${stats.flaky ?? 0}`);
          } catch (error) {
            console.log(`Could not read ${file}: ${error.message}`);
          }
          NODE
          exit $code
      - name: Upload mobile evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: mobile-evidence
          if-no-files-found: ignore
          path: |
            mobile.json
            server.log
            test-results/**
            playwright-report/**

  run-6-browser-forms:
    name: Run 6 — Browser forms and WhatsApp messages
    needs: run-1-static-validation
    if: ${{ always() }}
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
        with:
          ref: preview
      - uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
      - run: npm install
      - name: Install Playwright Chromium with timeout
        run: timeout 300s npx playwright install --with-deps chromium
      - name: Start local static server
        run: |
          npx http-server . -p 4173 -s > server.log 2>&1 &
          for i in {1..30}; do
            if curl -fsS $TEST_BASE_URL/index.html > /dev/null; then
              echo "Static server is ready."
              exit 0
            fi
            sleep 1
          done
          echo "Static server did not start."
          cat server.log
          exit 1
      - name: Run form workflow tests
        run: |
          set +e
          npx playwright test tests/essential-site.spec.js --project=chromium --grep "\\[forms\\]" --reporter=json > forms.json
          code=$?
          node - forms.json <<'NODE'
          const fs = require('fs');
          const file = process.argv[2];
          try {
            const report = JSON.parse(fs.readFileSync(file, 'utf8'));
            const stats = report.stats || {};
            console.log(`Playwright summary for ${file}: expected=${stats.expected ?? 0}, unexpected=${stats.unexpected ?? 0}, skipped=${stats.skipped ?? 0}, flaky=${stats.flaky ?? 0}`);
          } catch (error) {
            console.log(`Could not read ${file}: ${error.message}`);
          }
          NODE
          exit $code
      - name: Upload forms evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: forms-evidence
          if-no-files-found: ignore
          path: |
            forms.json
            server.log
            test-results/**
            playwright-report/**

  run-7-browser-contact:
    name: Run 7 — Browser contact CTA tests
    needs: run-1-static-validation
    if: ${{ always() }}
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
        with:
          ref: preview
      - uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
      - run: npm install
      - name: Install Playwright Chromium with timeout
        run: timeout 300s npx playwright install --with-deps chromium
      - name: Start local static server
        run: |
          npx http-server . -p 4173 -s > server.log 2>&1 &
          for i in {1..30}; do
            if curl -fsS $TEST_BASE_URL/index.html > /dev/null; then
              echo "Static server is ready."
              exit 0
            fi
            sleep 1
          done
          echo "Static server did not start."
          cat server.log
          exit 1
      - name: Run contact CTA tests
        run: |
          set +e
          npx playwright test tests/essential-site.spec.js --project=chromium --grep "\\[contact\\]" --reporter=json > contact.json
          code=$?
          node - contact.json <<'NODE'
          const fs = require('fs');
          const file = process.argv[2];
          try {
            const report = JSON.parse(fs.readFileSync(file, 'utf8'));
            const stats = report.stats || {};
            console.log(`Playwright summary for ${file}: expected=${stats.expected ?? 0}, unexpected=${stats.unexpected ?? 0}, skipped=${stats.skipped ?? 0}, flaky=${stats.flaky ?? 0}`);
          } catch (error) {
            console.log(`Could not read ${file}: ${error.message}`);
          }
          NODE
          exit $code
      - name: Upload contact evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: contact-evidence
          if-no-files-found: ignore
          path: |
            contact.json
            server.log
            test-results/**
            playwright-report/**
